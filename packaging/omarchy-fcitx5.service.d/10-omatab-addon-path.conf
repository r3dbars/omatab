# Fcitx is the keyboard; it must never wait on Ollama. Wants= starts the
# warmer alongside it at login without ordering.
[Unit]
Wants=omatab-model-warm.service

[Service]
Environment=FCITX_ADDON_DIRS=%h/.local/lib/fcitx5:/usr/lib/fcitx5
